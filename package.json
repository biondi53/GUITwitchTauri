{
  "name": "twitch-ultra-ligero",
  "private": true,
  "version": "1.1.57",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test src/*.test.js",
    "e2e": "node tests/e2e/run-e2e.mjs",
    "tauri": "tauri",
    "bump:patch": "node bump-version.js patch",
    "bump:minor": "node bump-version.js minor",
    "bump:major": "node bump-version.js major",
    "release:patch": "node bump-version.js patch && npm run tauri build -- --bundles nsis",
    "release:minor": "node bump-version.js minor && npm run tauri build -- --bundles nsis",
    "release:major": "node bump-version.js major && npm run tauri build -- --bundles nsis"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "playwright": "^1.62.1",
    "vite": "^6"
  }
}
